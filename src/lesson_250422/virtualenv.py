# preferences -> Project -> Project Interpreter -> virtualenv 설정 예) python2 버전
# virtualenv로 실행하면 다른 버전의 파이썬에서는 에러나는 것도 가능함
# 예) python2에서는 print 'test'를 실행하면 test를 출력하지만
#    python3에서는 print('test')를 해야함 -> 괄호 없으면 에러
